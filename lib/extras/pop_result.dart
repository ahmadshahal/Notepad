import 'package:flutter/material.dart';

class PopResult {
  bool save;
  bool delete;
  String title;
  String text;
  Color color;

  PopResult({this.save, this.delete, this.text, this.title, this.color});
}
